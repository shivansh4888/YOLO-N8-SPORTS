# ─────────────────────────────────────────────────────────────────
# Multi-Object Tracking — PRO EDITION
# Install: pip install -r requirements.txt
# NEW vs v1: boxmot (StrongSORT+OSNet), easyocr, pandas
# ─────────────────────────────────────────────────────────────────

# Core detection
ultralytics>=8.0.0

# Unified tracking library — StrongSORT, BoT-SORT, ByteTrack
# OSNet Re-ID weights download automatically on first run
boxmot>=10.0.0

# Annotation helpers (bounding boxes, colours, etc.)
supervision>=0.21.0

# Video I/O
opencv-python-headless>=4.8.0

# Numerics
numpy>=1.24.0
scipy>=1.10.0

# Plots & heatmaps
matplotlib>=3.7.0

# Jersey number OCR — handles blurry low-res crops
easyocr>=1.7.0

# Player stats export
pandas>=2.0.0

# Deep learning backend
torch>=2.0.0
torchvision>=0.15.0
# GPU users: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# Video download
yt-dlp>=2024.1.0

# Progress bars
tqdm>=4.65.0