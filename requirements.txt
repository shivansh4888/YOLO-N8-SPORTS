# ─────────────────────────────────────────────────────────────────
# Sports MOT — CPU-optimised full feature set
# pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# Detection
ultralytics>=8.2.0          # YOLOv8 — also includes BoT-SORT tracker

# Tracking + annotation utilities
supervision>=0.21.0

# Video I/O
opencv-python-headless>=4.9.0

# Numerics / signal processing
numpy>=1.26.0
scipy>=1.12.0               # gaussian_filter, DBSCAN via sklearn

# Face detection + embedding (CPU-friendly, ~50ms/face)
# insightface uses ONNX Runtime — runs fast on CPU
# insightface>=0.7.3
onnxruntime>=1.17.0         # CPU ONNX backend for insightface

# Jersey number OCR
# easyocr>=1.7.1              # runs every 20 frames, fine on CPU

# Plots, heatmaps, stats
matplotlib>=3.8.0
pandas>=2.1.0
scikit-learn>=1.4.0         # DBSCAN for face clustering

# Video download
yt-dlp>=2024.3.0

# Progress bars
tqdm>=4.66.0

# Deep learning backend (CPU torch — lighter install)
torch>=2.1.0
torchvision>=0.16.0
# If you have NVIDIA GPU later:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# ── Core ML / CV ──────────────────────────────────────────────────
ultralytics>=8.2.0          # YOLOv8 + BoT-SORT tracker built-in
supervision>=0.21.0         # Detection/tracking helpers
opencv-python-headless>=4.9 # Headless: no GUI deps (important on servers)
numpy>=1.26
# easyocr>=1.7.1              # Jersey OCR

# ── Web framework ─────────────────────────────────────────────────
flask>=3.0
gunicorn>=22.0.0            # Production WSGI server

# ── Video download ────────────────────────────────────────────────
yt-dlp>=2024.11.18          # YouTube downloader (fork of youtube-dl)

# ── Analytics / export ────────────────────────────────────────────
pandas>=2.2
matplotlib>=3.9
scipy>=1.13                 # Used by heatmap generation

# ── Optional: InsightFace for face clustering ─────────────────────
# Uncomment if ENABLE_FACE_ID = True in config.py
# insightface>=0.7.3
# onnxruntime>=1.18          # CPU inference backend for InsightFace